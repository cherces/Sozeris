<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Sozeris.Pages.HomePage"
             Padding="0">
    <AbsoluteLayout>
        <Image Source="welcome_page_background.png"
               Aspect="AspectFill"
               AbsoluteLayout.LayoutBounds="0,0,1,1"
               AbsoluteLayout.LayoutFlags="All"
               IsEnabled="False" />
        <VerticalStackLayout VerticalOptions="Center"
                             Spacing="40"
                             Padding="30"
                             AbsoluteLayout.LayoutBounds="0,0,1,1"
                             AbsoluteLayout.LayoutFlags="All">
            <Label Text="ХлебКаждыйДень"
                   FontSize="36"
                   TextColor="White"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

            <Label Text="Свежий хлеб — каждый день"
                   FontSize="18"
                   TextColor="White"
                   HorizontalOptions="Center" />

            <Button Text="Начать"
                    FontSize="20"
                    BackgroundColor="#D97B29"
                    TextColor="White"
                    CornerRadius="12"
                    HeightRequest="50"
                    Clicked="OnStartClicked" />
        </VerticalStackLayout>
    </AbsoluteLayout>
</ContentPage>